---
- alias: Zone Alerts - Steph
  trigger:
    - platform: event
      event_type: state_changed
      data:
        entity_id: person.steph

  action:
    - service: notify.bertie_iphone
      data:
        message: Steph's location has changed to 'Hello'
