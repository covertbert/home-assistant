---
- alias: Away Mode
  trigger:
    platform: state
    entity_id: group.family
    to: not_home
  action:
    - service: notify.bertie_iphone
      data:
        message: 🚗💨 Home state set to 'Away'
    - service: input_select.select_option
      data:
        entity_id: input_boolean.home_state
        option: Away
